EXEC = qualquercoisa
CC = gcc
INC = ./includes/
FLAGS = -Wall -Wextra -Werror
CFILES = ${INC}ft_putchar.c ./ex06/ft_print_alphabet.c ${INC}main.c
DEL = rm -f

all:
	${CC} ${FLAGS} ${CFILES} -o ${EXEC}

clean:
	${DEL} *.o

fclean: clean
	${DEL} ${EXEC} 

re: fclean all
